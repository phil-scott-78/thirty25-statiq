{
  "name": "thirty25-statiq",
  "version": "1.0.0",
  "description": "thirty25.com blog with statiq",
  "main": "index.js",
  "scripts": {
    "install-dotnet": "./dotnet-install.sh --channel 6.0 --install-dir ~/.net/",
    "build": "if-env VERCEL=1 && npm run build:vercel || npm run build:dev",
    "build:vercel": "npm run install-dotnet && cross-env NODE_ENV=production DOTNET_PATH=\"/vercel/.net/dotnet\" ASPNETCORE_URLS=\"http://localhost:5500\" ~/.net/dotnet run --urls \"http://localhost:322021\"",
    "build:dev": "dotnet run",
    "watch": "dotnet run -- preview"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "if-env": "^1.0.4"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
